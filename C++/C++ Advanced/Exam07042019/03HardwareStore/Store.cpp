
#include "pch.h"
#include "Store.h"
#

void Store::remove(const int index) {
	_laptops.erase[index];
}

void Store::copy(const int fromIndex, const int toIndex) {
	for (int i = fromIndex; i <= toIndex; i++) {

	}
}